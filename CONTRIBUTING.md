# 贡献指南

感谢你为 nasa-redis 提交改进。开始修改前，请先在 GitHub Issue 中说明问题背景、兼容性影响和预期行为；小型且边界清晰的修正可以直接提交 Pull Request。

## 开发环境

- JDK 21 或更高版本
- Maven 3.6.3 或更高版本

构建命令：

```bash
mvn -B -ntp clean verify
```

## 代码要求

- 保持核心库为独立的纯 Java 项目，不引入应用容器耦合。
- 公共 API 变更必须说明二进制兼容性和线路协议兼容性影响。
- 注释使用中文，代码标识符、协议名和配置键保留原文。
- 方法注释需写明业务作用；有业务含义的参数和返回值应说明边界、失败条件与副作用。
- 注释不得记录工具来源、自动生成来源或临时迭代标记，只保留当前实现的设计原因与约束。
- 提交内容只包含产品源码、公开资源和必要的发布文档；本地质量工程及其产物不进入产品提交或发布归档。
- 不提交生成目录、IDE 元数据、密钥、令牌、日志或本机配置。

## 提交说明

提交信息应简洁说明“改了什么”和“为什么”。Pull Request 需包含变更摘要、兼容性说明以及必要的人工验证记录。

提交贡献即表示你同意该贡献按照 Apache-2.0 OR MIT 双许可证发布。
