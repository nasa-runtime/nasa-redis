# 贡献指南

感谢你为 nasa-redis 提交改进。开始修改前，请先在 GitHub Issue 中说明问题背景、兼容性影响和预期行为；小型且边界清晰的修正可以直接提交 Pull Request。

## 开发环境

- JDK 21 或更高版本
- Maven 3.6.3 或更高版本

构建命令：

```bash
mvn -B -ntp clean verify
```

## 代码要求

- 保持基础设施能力不依赖具体业务应用；Spring 配置与生命周期集成必须通过公开、可验证的装配入口完成。
- 公共 API 变更必须说明二进制兼容性和线路协议兼容性影响。
- 注释使用中文，代码标识符、协议名和配置键保留原文。
- 方法注释必须包含“业务作用”，并准确说明职责、边界或安全意义；有业务含义的参数和返回值还应说明成功
  条件、失败条件、状态变化与副作用。没有显式业务参数时应写明“参数说明: 无。”。
- 注释用于说明业务意图、安全不变量、设计原因、边界条件和失败后果，不逐句复述代码。保护态切换、租约或
  执行权复验、fencing、外部副作用、补偿、退避以及“先持久提交后开放路由”等关键顺序，必须在对应步骤前
  说明原因和安全影响。
- 注释和公开文档只保留当前有效的产品合同，不记录工具归因、开发批次、工作流水、短期诊断内容或不再成立
  的入口；公开 API、协议字段、配置键与业务状态名称必须和实现一致。
- 不使用内部里程碑或开发章节编号组织注释、文档与默认迁移文件名；沿革信息由 Git 提交记录承载。
- 提交内容只包含产品源码与公开资源；本机生成物、临时工具及其产物不进入产品提交或发布归档。
- 不提交生成目录、IDE 元数据、密钥、令牌、日志或本机配置。

## 文档与归档

README、组件说明、Javadoc、POM 元数据、示例和配置共同构成公开合同。改变能力定位、配置默认值、状态顺序、
兼容边界或失败语义时，必须同步更新所有受影响入口。提交前应执行 [公开交付清单](RELEASE-CHECKLIST.md)，
并以构建生成的最终归档内容为准核对文件、版本、许可证与文档。

## 提交说明

提交信息应简洁说明“改了什么”和“为什么”。Pull Request 需包含变更摘要、兼容性说明与使用边界。

提交贡献即表示你同意该贡献按照 Apache-2.0 OR MIT 双许可证发布。
